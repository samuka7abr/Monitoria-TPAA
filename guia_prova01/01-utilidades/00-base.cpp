// g++ file.cpp -o prog -std=c++17 -Wfatal-errors
// ./prog < in

#include <bits/stdc++.h>
using ll = long long; //opcionais, são  alias para long long e unsigned long long
using ull = unsigned long long; //opcionais, são  alias para long long e unsigned long long

using namespace std;
int main() {
    //para otimizar a entrada e saida de dados, podemos usar o seguinte:
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    return 0;
}
